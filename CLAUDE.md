# 하루도장 — 프로젝트 규칙

도장깨기식 생활 루틴앱. 기획 완료(2026-07-06), 계획은 사용자 승인됨 (HARD-GATE 충족 — 바로 구현 가능).

## 시작 절차

1. `docs/01-concept.md` → `docs/04-tech-spec.md` → `docs/05-roadmap.md` 순으로 읽는다.
2. 현재 진행 중인 마일스톤을 파악한다 (README.md의 상태 섹션 + git log).
3. 아래 모델 분담에 따라 작업을 위임한다. **사용자에게 모델 선택을 묻지 않는다 — 이미 결정된 사항이다.**

## 모델 분담 (필수 — 사용자가 수동 전환하지 않도록 자동 적용)

구현 작업은 메인 세션이 직접 하지 말고, Agent 도구로 아래 모델의 서브에이전트에 위임한다:

| 작업 | Agent 도구 `model` 파라미터 |
|------|---------------------------|
| M1 판정 엔진·상태 전이 (TDD) | `opus` |
| M1 콘텐츠 전사 (02문서 → courses.ts, journal.ts) | `haiku` |
| M2, M3, M4, M5 구현 | `sonnet` |
| 마일스톤 완료 후 코드 리뷰 | `opus` |
| M4 상태 전이 등 막히는 난제 | `opus` (해당 부분만 재위임) |

메인 세션의 역할 (오케스트레이터):
- 위임 프롬프트에 관련 docs 경로와 완료 기준을 명시해서 전달
- 서브에이전트 완료 후 **직접 테스트/빌드를 실행해 독립 검증** (에이전트 자기 보고 신뢰 금지)
- 검증 통과 후 커밋, README.md의 상태 섹션 갱신

## 작업 규칙

- 마일스톤당 세션 하나 (컨텍스트 50% 규칙). 마일스톤 완료 시 상태를 README.md에 기록하고 세션 종료를 제안한다.
- M1은 TDD 필수: 테스트 먼저 (RED → GREEN → IMPROVE). domain/ 커버리지 90%+.
- 콘텐츠 텍스트(관문 이름, 일지 25편)의 원본은 `docs/02-content-design.md`, UI 문구 전체의 원본은 `docs/06-microcopy.md`. 코드로 옮길 때 임의 수정·창작 금지. 06문서의 금지 목록(이모지·느낌표·응원 문구 등)을 위반하지 말 것.
- 날짜 계산은 date-fns만 사용. dayKey 규칙(하루 경계 04시)은 `docs/04-tech-spec.md` 명세를 따른다.
- 미결 사항(README.md 참고)을 만나면 그때 사용자에게 확인.
- 완료 선언에는 실행 증거 필수: 테스트 통과 수, 빌드 exit 0.
